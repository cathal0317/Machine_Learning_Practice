---
id: obj_165
title: "3.5 Generative adversarial networks"
types:
  - subsection
page_start: 70
page_end: 73
parent_id: "obj_134"
children_ids:
  - obj_166
  - obj_168
sibling_ids:
  - obj_133
  - obj_141
  - obj_143
  - obj_159
  - obj_170
prerequisites:
  - obj_141
used_in: []
analogous_to:
  - obj_170
same_pattern_as: []
family: "section-family"
---

# 3.5 Generative adversarial networks

## Conceptual overview
Generative Adversarial Networks (GANs) are a generative modeling framework where two networks, a Generator $G$ and a Discriminator $D$, compete in a min-max game. The generator attempts to produce realistic samples from a latent distribution, while the discriminator tries to distinguish them from real data.

## Why it matters
They provide a way to learn and sample from high-dimensional data distributions, such as images, without explicitly defining the probability density function.

## Active recall
> [!question]- What are the two main components of a GAN and their respective goals?
> The Generator $G$ aims to generate data indistinguishable from the true distribution $\rho_X$. The Discriminator $D$ aims to estimate the probability that a sample came from the real data rather than the generator.

> [!question]- State the min-max objective function $V(D, G)$ for GANs.
> $\min_G \max_D V(D, G)$ where $V(D, G) = \mathbb{E}_{X \sim \rho_X}(\log(D(X))) + \mathbb{E}_{Z \sim \rho_Z}(\log(1 - D(G(Z))))$.

## Narrative flow
The subsection defines the generative modeling problem, introduces the GAN components, formalizes the min-max value function, establishes the connection to the Bayes classifier, and provides a training algorithm.

## Links
**Parent:** [[obj_134 - Chapter 3 Neural networks]]

**Children:**
- [[obj_166 - Theorem 25]]
- [[obj_168 - Algorithm for GAN optimization]]

**Prerequisites:**
- [[obj_141 - 3.2 Cross-entropy loss]]

**Analogous to:**
- [[obj_170 - 3.6 Variational autoencoders]]
